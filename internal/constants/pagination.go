package constants

// Pagination constants
const (
	// NoLimit indicates no pagination should be applied
	NoLimit = 0
	// DefaultPage is the default page number if not specified
	DefaultPage = 1
	// DefaultPageSize is the default page size if not specified
	DefaultPageSize = 10
)
